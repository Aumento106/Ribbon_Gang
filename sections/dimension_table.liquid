<style>
.medibin-table-wrapper {
  padding: 2rem 1rem;
  overflow-x: auto;
}
.medibin-table {
  display: table;
  width: 100%;
  border-collapse: collapse;
}
.table-row {
  display: table-row;
}
.table-cell {
  display: table-cell;
  padding: 12px;
  border: 1px solid #ddd;
  vertical-align: middle;
}
.table-head .table-cell {
  font-weight: bold;
  background-color: #f7b500;
  color: white;
}
.table-row:nth-child(even):not(.table-head) {
  background-color: #f9f9f9;
}

@media screen and (max-width: 640px) {
  .medibin-table {
    display: block;
  }
  .table-row {
    display: block;
    margin-bottom: 1rem;
  }
  .table-cell {
    display: block;
    padding: 8px;
    border: none;
    border-bottom: 1px solid #ddd;
  }
  .table-head {
    display: none;
  }
}
</style>

<section class="page-width medibin-table-wrapper">
  <div class="medibin-table">
    <div class="table-row table-head">
      {% if section.settings.description != blank %}
        <div class="table-cell">{{ section.settings.description }}</div>
      {% endif %}
      {% if section.settings.code != blank %}
        <div class="table-cell">{{ section.settings.code }}</div>
      {% endif %}
      {% if section.settings.dimensions != blank %}
        <div class="table-cell">{{ section.settings.dimensions }}</div>
      {% endif %}
      {% if section.settings.dimensions_inch != blank %}
        <div class="table-cell">{{ section.settings.dimensions_inch }}</div>
      {% endif %}
    </div>

    {% for row in section.blocks %}
      <div class="table-row">
        {% if section.settings.description != blank %}
          <div class="table-cell">{{ row.settings.description }}</div>
        {% endif %}
        {% if section.settings.code != blank %}
          <div class="table-cell">{{ row.settings.code }}</div>
        {% endif %}
        {% if section.settings.dimensions != blank %}
          <div class="table-cell">{{ row.settings.dimensions }}</div>
        {% endif %}
        {% if section.settings.dimensions_inch != blank %}
          <div class="table-cell">{{ row.settings.dimensions_inch }}</div>
        {% endif %}
      </div>
    {% endfor %}
  </div>
</section>

{% schema %}
{
  "name": "dimension-table",
  "settings": [
    {
      "type": "text",
      "id": "description",
      "label": "Description",
      "default": "Description"
    },
    {
      "type": "text",
      "id": "code",
      "label": "Code",
      "default": "Code"
    },
    {
      "type": "text",
      "id": "dimensions",
      "label": "Dimensions",
      "default": "Dimensions"
    },
    {
      "type": "text",
      "id": "dimensions_inch",
      "label": "Dimensions Inches",
      "default": "Dimensions (inches)"
    }
  ],
  "blocks": [
    {
      "type": "row",
      "name": "Row",
      "settings": [
        {
          "type": "text",
          "id": "description",
          "label": "Description",
          "default": "Extra Small MEDIBIN™"
        },
        {
          "type": "text",
          "id": "code",
          "label": "Code",
          "default": "10000XS"
        },
        {
          "type": "text",
          "id": "dimensions",
          "label": "Dimensions",
          "default": "100W × 120H × 215D"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "dimension-table",
      "category": "Custom"
    }
  ]
}
{% endschema %}

